# The four code repos live here as siblings, not as part of this repo.
repos/

# OS
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
.claude/

# Logs
*.log

# Personal ride data (never commit anywhere). Anchored to repo root so it
# doesn't shadow docs/sessions/, which is the session-log archive.
*.fit
*.tcx
*.gpx
/sessions/
/rides/
